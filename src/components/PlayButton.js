import React from 'react';

const PlayButton = () => (
  <div>
    <button className="button bg-transparent contain button-reset play-button db bg-contain bg-center pointer center" />
  </div>
);

export default PlayButton;
